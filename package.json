{
  "name": "login-unit-test",
  "version": "1.0.0",
  "description": "Login function with separate smoke and regression tests",
  "main": "auth.js",
  "scripts": {
    "test:smoke": "jest auth.smoke.test.js",
    "test:regression": "jest auth.regression.test.js"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  }
}
